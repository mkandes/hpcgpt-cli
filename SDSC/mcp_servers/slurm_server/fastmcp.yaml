name: fastmcp

channels:
  - conda-forge

dependencies:
  - python
  - jupyterlab
  - ipywidgets
  - jupyterlab-lsp
  - pip
  - pip:
    - python-lsp-server
    - fastmcp
    - requests
    - pydantic>=2
    - rich-argparse
